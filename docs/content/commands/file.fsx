(**
## fix file

File manipulation commands

     file [add <string>] [remove <string>] [list] [order <string> <strign>]

### Options

`add <string>`: Adds a file to the current folder and project.

`remove <string>`: Removes a file from the current folder and project.

`list`: List all files of the current project.

`order <string> <string>`:  orders `file1` immediately before `file2` in the project.

*)